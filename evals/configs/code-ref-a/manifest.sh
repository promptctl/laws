# Configuration manifest - DATA ONLY. Names which guidance at which version; no task, no repo,
# no criterion. The body is read straight from git at CONFIG_REF at resolve time.
CONFIG_SKILL="code"
CONFIG_REF="8f6d15b"
CONFIG_SUMMARY="laws:code body at ref 8f6d15b (parse-dont-validate restored)"
