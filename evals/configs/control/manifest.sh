# Configuration manifest - DATA ONLY. The control arm: no skill loaded at all. Empty CONFIG_SKILL
# and no CONFIG_REF. Resolving this yields no body - the baseline an edited skill is measured
# against.
CONFIG_SKILL=""
CONFIG_REF=""
CONFIG_SUMMARY="control arm - no skill body loaded"
