# Configuration manifest - DATA ONLY. Same skill as code-ref-a, a DIFFERENT git ref: this is the
# whole point of the arm - two versions of the same guidance, so an edit can be compared.
CONFIG_SKILL="code"
CONFIG_REF="58b573b"
CONFIG_SUMMARY="laws:code body at ref 58b573b (before the parse-dont-validate restoration)"
