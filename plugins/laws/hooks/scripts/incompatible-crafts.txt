# Incompatible craft pairs — the authoritative compatibility policy for the laws plugin.
#
# One symmetric pair per line, bare craft names (no "laws:" prefix). Two crafts named on
# one line corrupt each other's standard when both are loaded, so the enforcers refuse or
# switch away from the second; EVERY pairing absent from this list may coexist. This is a
# SHORT list by design — not a full NxN matrix, and not an "exclusive" craft — naming only
# a pairing shown to break real work. Add an edge only with the same kind of evidence.
#
# Read by BOTH enforcers, so the rule has exactly one home ([LAW:one-source-of-truth]):
#   - skill-router.sh  — the PreToolUse guard that refuses the incompatible 2nd load
#   - laws-excise.js   — the runtime gate that offers the four-choice switch instead
# Changing the policy is editing THIS file; neither enforcer hard-codes a craft name.
#
# Blank lines and lines beginning with '#' are ignored; a trailing '# comment' is stripped.

# code is nothing like prompt-for-an-LLM — each craft's rules are actively wrong for the
# other's medium — so stacking the two corrupts both. (chat, prose, ticket, and
# application-spec are in no incompatible pair, so they mix freely with each other and
# with code.)
code prompt
