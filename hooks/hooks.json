{
  "description": "Once per session: route to the one medium-matched skill (laws:code / laws:prompt / laws:prose) and command active engagement with it. An optional cooldown-gated 'engage' re-activation ping exists in the script, unwired pending real-world testing.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/skill-router.sh\" session-start"
          }
        ]
      }
    ]
  }
}
