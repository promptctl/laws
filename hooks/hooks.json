{
  "description": "Three injection points: SessionStart routes once to the medium-matched skill (laws:code / laws:prompt / laws:prose) and resets the medium lock on a fresh session; UserPromptSubmit re-engages the laws on every user message; PreToolUse on Skill guards one-craft-per-session, refusing a second medium load.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/skill-router.sh\" session-start"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/skill-router.sh\" engage"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/skill-router.sh\" guard"
          }
        ]
      }
    ]
  }
}
